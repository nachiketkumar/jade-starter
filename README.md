You must have jade and gulp installed:

`$ npm install jade`

`$ npm install —g gulp`

Clone the project and inside the project directory, run:

`$ npm install`

To serve the project, run:

`gulp serve`